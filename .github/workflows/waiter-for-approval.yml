name: Run tests
on: 
  pull_request_target:
    branches:
      - 'master'
      
jobs:
  build_and_test:
    uses: ./.github/workflows/build_and_test_ondemand.yml
    if: github.event.review.state == 'approved'
    secrets: inherit
